export type BlogViewModel = {
    // а можно изменить регистр букв в названии??
    id: string;
    name: string;
    description: string;
    websiteUrl: string;
    createdAt: Date;
    isMembership: boolean;
};